#include <bits/stdc++.h>
using namespace std;
vector<int> readArrays();                      // 读取一维数组
vector<vector<int>> readMatrix();              // 读取二维数组
void printArray(vector<int> arr);              // 打印一维数组
void printMatrix(vector<vector<int>> matrix);  // 打印二维数组